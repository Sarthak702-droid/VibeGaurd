# Compatibility tests

The Rust integration suite exercises legacy command names and output artifact
contracts against fixture repositories. `vig` is intentionally excluded: the
Rust distribution exposes only `vibeguard` and `vbg`.

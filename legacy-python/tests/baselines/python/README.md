# Python golden baselines

Baselines are normalized terminal and artifact outputs captured from the legacy
Python implementation. Volatile timestamps, absolute paths, durations, and
machine-specific executable locations are replaced during comparison. Fixtures
contain only non-secret test values.

# ADR 0002: Rust is the core runtime

Status: accepted

The Python package is retained temporarily as `legacy-python/` for behavior
comparison. It is not used by the Rust CLI or release workflow. Any future
Python capability must be an optional, versioned stdin/stdout AI sidecar.
